services:
  simplayce-mvp:
    build: .
    container_name: simplayce-mvp
    ports:
      - "8503:8503"
    restart: unless-stopped
    environment:
      - STREAMLIT_SERVER_PORT=8503
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
